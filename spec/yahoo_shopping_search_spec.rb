RSpec.describe YahooShoppingSearch do
  it "has a version number" do
    expect(YahooShoppingSearch::VERSION).not_to be nil
  end

  xit "does something useful" do
    expect(false).to eq(true)
  end
end
